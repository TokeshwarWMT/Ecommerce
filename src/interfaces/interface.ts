export interface idecodedToken {
    id: string;
};

export interface iProduct {
    userId: string;
};

export interface iDecodedToken {
    id: string;
};

export interface iCart {
    userId: string;
};